# runtime data: config + compiled-module cache
/data/

# build artifacts
/fanctld
/fnos-fan
*.ko
*.o
*.mod.c

# release artifacts
/dist/
.tmp-kmod/
.tmp-it87/

# nested clone left in vendored source (safe to delete: rm -rf kmod/<m>/.git)
kmod/qnap8528/.git/
kmod/it87/.git/
